apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: mosquitto
  name: mosquitto
spec:
  ports:
    - name: "1883"
      port: 1883
      targetPort: 1883
  selector:
    app.kubernetes.io/component: mosquitto
